<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body{
            width: 100%;
            height: 100%;
            margin: 0px;
        }
        canvas{
            width: 100%;
            height: 100%;
            margin: 0px;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <canvas id="animCanv"></canvas>
</body>
<script>

    var c = document.getElementById("animCanv");
    c.width = window.innerWidth;
    c.height = window.innerHeight;

    var ctx = c.getContext("2d");

    function character(args){
        this.path = args.path;
        for(let i of this.path){
            i.maxTime = i.time;
        }
        this.conversation = args.conversation;
        this.name = args.name;
        this.currentStep = 0;
        this.x = this.path[0].x;
        this.y = this.path[0].y;
        function update(){
            if(this.currentStep < this.path.length){

            }
        }
        function draw(){
            
        }
    }

    function lerp(a, b, w){
        return (a + (b-a)*w);
    }

    function lerp2d(a, b, w){
        return {x: a.x + (b.x-a.x)*w, y:a.y + (b.y-a.y)*w};
    }
</script>
</html>
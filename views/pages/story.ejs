<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/ws'); %>
        <script>
            const url = 'wss://kahootpod.com:2083';
            const ws = new WebSocket(url);
            ws.onopen = () => {
            }
            ws.onerror = error => {
                console.log(`WebSocket error: ${error}`)
            }
        </script>
    </head>
    <body>
        <p>Test</p>
        <%for(var i = 0; i < 10; i++){%>
        <%=i%>
        <%}%>
    </body>
</html>


